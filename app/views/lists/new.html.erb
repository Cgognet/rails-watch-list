<div class="container">
  <div class="row mt-4">
    <%= simple_form_for (@list) do |f| %>
      <%= f.input :name %>
      <%= f.input :image_url %>
      <%= f.submit "Submit review", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
